# Lists in python

# list is a collection of elements, which is ordered and mutable
# the elements can be of different data types

table = [5*i for i in range(1,11)]
print(table)

table_another = [2*i for i in range(1,11)]
print(table_another)